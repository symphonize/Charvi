<% provide(:title, 'Companies') %>
<h1>Companies</h1>

<ul class="companies">
  <% @companies.each do |company| %>
    <li>
      <%= link_to company.name, company %>
       | <%= link_to "edit", edit_company_path(id: company.id)%>
    </li>
  <% end %>
</ul>