<% provide(:title, 'All users') %>
<div class="white-panel pad10">
<h3>All users</h3>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>      
      <%= link_to user.name, user %>  | <%=user.email%>
    </li>    
  <% end %>
</ul>

<%= will_paginate %>

</div>
