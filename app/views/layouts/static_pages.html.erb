<!DOCTYPE html>
<html>
  <head>  	
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield :head%>
  </head>
  <body>  	
  	<div class="head-float">
  		<%= render 'layouts/header' %>
  	</div>
  	<div id="myCarousel" class="carousel slide">
		<div class="carousel-inner">
	    	<div class="item active">
	        	<img src="/assets/banner.jpg" alt="">
	          	<div class="container">
	        	<div class="carousel-caption">
	              		<div class="welcome">Welcome to BYT.</div>
	              		<p class="lead">The small business Invoicing App.</p>
	              		<%= link_to raw("<i class='icon-user icon-white'></i> Sign up now!"), signup_path, class: "btn btn-large btn-primary" %>
	            	</div>
	          	</div>
	        </div>
		</div>
	</div>
	<div class="container">
		<%= yield%>
	</div>	
    <%= render 'layouts/footer' %>
  	<%= debug(params) if Rails.env.development? %>	    
  </body>
</html>